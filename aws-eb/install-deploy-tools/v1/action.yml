name: Install deploy tools
description: "Install tools for deployment"

inputs:
  python-version:
    description: "Version of installed python"
    required: false
    default: "3.12"

runs:
  using: "composite"
  steps:
    - name: Set up python
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
      with:
        python-version: ${{ inputs.python-version }}

    - name: Cache pip packages
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: ~/.cache/pip
        key: pip-deploy-tools-${{ runner.os }}-ebi-awsebcli3.27.2

    - name: Install ebi and awsebcli
      shell: bash
      run: |
        pip install -q -U ebi 'awsebcli==3.27.2'

        # Check commands
        eb --version
        ebi --help

    - name: Install yq
      shell: bash
      working-directory: /tmp
      run: |
        wget -q https://github.com/mikefarah/yq/releases/download/v4.53.2/yq_linux_amd64.tar.gz -O - | tar xz
        sudo mv yq_linux_amd64 /usr/local/bin/yq

        # Check command
        yq -V
